apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-go
spec:
  selector:
    matchLabels:
      run: hello
  template:
    metadata:
      labels:
        run: my
    spec:
      containers:
        - name: hello-go
          image: hello-go
          ports:
            - containerPort: 8080